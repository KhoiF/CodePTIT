#include<bits/stdc++.h>

using namespace std;
signed main()
{
    ios_base::sync_with_stdio(false);cin.tie(0);cout.tie(0);
    freopen("PTIT.in" , "r" , stdin);
    freopen("PTIT.out" , "w" , stdout);
    string s;
    while(getline(cin,s)){
        cout << s << endl;
    }
}